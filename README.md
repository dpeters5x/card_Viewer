# card_Viewer

A small static web app for browsing and arranging playing-card SVG assets.

- `index.html` — SVG card browser
- `hand.html` — drag-and-drop card hand reordering
- `svg_cards/`, `svg_cards2/` — SVG card asset sets

No build step — just open the HTML files directly in a browser.
